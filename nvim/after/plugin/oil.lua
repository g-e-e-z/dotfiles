require("oil").setup({
    float = {
        -- Padding around the floating window
        padding = 5,
        -- max_width = 0,
        -- max_height = 0,
        border = "rounded",
        win_options = {
            winblend = 0,
        },
    }
})
