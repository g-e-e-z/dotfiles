views:
  # Alters the pod view column layout. Uses GVR as key
  v1/pods:
    columns:
      - NAME
      - PF
      - READY
      - STATUS
      - "%CPU/R"
      - "%MEM/R"
      - RESTARTS
      - CPU
      - MEM
      - IP
      - NODE
